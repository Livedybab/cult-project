# 🎨 UI Style Guide & Design System Specification

Данный документ описывает правила визуального оформления, цветовую палитру, компоненты и принципы UI/UX для разработчиков и дизайнеров проекта **Cult Connection**.

---

## Главный принцип визуала

Дизайн построен на **тёмной премиальной палитре** с холодным фиолетово-розовым свечением. Основа глубокая сине-чёрная, с мягкими неоновыми акцентами.

**Пропорции распределения цвета (Правило 75/15/7/3):**
* **75%** - тёмный сине-чёрный фон;
* **15%** - графитовые карточки и блоки;
* **7%** - белый и серый текст;
* **3%** - фиолетовые, пурпурные и розовые акценты.

> **Правило:** Ограниченное количество яркого цвета создаёт премиальный вид. Фиолетовый служит акцентом, а не сплошной заливкой. Чистый `#000000` не используется.

---

## Цветовая палитра

### 1. Сводная таблица токенов

| Назначение | CSS Переменная | HEX / Value | Описание / Использование |
| :--- | :--- | :--- | :--- |
| **Main Background** | `--bg-main` | `#07101A` | Основной сине-чёрный фон приложения (мягче обычного чёрного). |
| **Darkest Background** | `--bg-darkest` | `#050B12` | Самый тёмный подстилающий слой. |
| **Screen Background** | `--bg-screen` | `#0A1420` | Фон отдельных экранов. |
| **Card Primary** | `--card-primary` | `#0D1824` | Основной фон карточек (не требует тяжелых рамок). |
| **Card Secondary** | `--card-secondary` | `#111D2B` | Поля ввода, настройки, списки чатов, ИИ-меню. |
| **Card Active** | `--card-active` | `#17172B` | Активные карточки с фиолетовым подтоном. |
| **Brand Primary** | `--accent-purple` | `#8338EC` | Главный акцент (кнопки, прогресс, активные теги). |
| **Bright Purple** | `--accent-bright` | `#A855F7` | Светлый акцент для свечения и градиентов. |
| **Pink / Magenta** | `--accent-pink` | `#E449D7` | Кольцо совместимости, сердечки, точечные блики. |
| **Dark Purple** | `--purple-dark` | `#54218F` | Тёмный край градиентов. |
| **Text Primary** | `--text-primary` | `#F7F7FB` | Заголовки, имена, ключевые цифры (холодный белый). |
| **Text Secondary** | `--text-secondary` | `#A6ADBA` | Описания, мета-информация, вспомогательные подписи. |
| **Text Muted** | `--text-muted` | `#687280` | Неактивные элементы, время сообщений, даты. |
| **Text Accent** | `--text-accent` | `#D8B4FE` | Лавандовый текст (ИИ-статусы, плашки Premium). |
| **Status Online** | `--status-online` | `#3EE98A` | Индикатор онлайна и успешных действий. |
| **Status Danger** | `--status-danger` | `#FF4D67` | Ошибки, предупреждения, удаление. |
| **Like Button** | `--accent-like` | `#F43F75` | Кнопка «Лайк». |

---

## Градиенты, Свечения и Границы

### Градиенты (Gradients)
* **Основной градиент (Кнопки, проценты, акценты):**
  ```css
  background: linear-gradient(135deg, #6D28D9 0%, #9333EA 45%, #D946EF 100%);
  ```
* **Яркий акцентный градиент:**
  ```css
  background: linear-gradient(135deg, #7C3AED 0%, #A855F7 50%, #EC4899 100%);
  ```
* **Градиент кнопки «Лайк»:**
  ```css
  background: linear-gradient(135deg, #EC4899 0%, #F43F5E 100%);
  ```
* **Кольцо совместимости (Conic):**
  ```css
  background: conic-gradient(from 210deg, #7C3AED, #D946EF, #F052C5, #7C3AED);
  ```

### Свечения (Glow Effects)
* **Основное фиолетовое свечение:**
  `box-shadow: 0 0 18px rgba(147, 51, 234, 0.32), 0 0 42px rgba(147, 51, 234, 0.14);`
* **Свечение активной кнопки:**
  `box-shadow: 0 6px 24px rgba(147, 51, 234, 0.38), 0 0 12px rgba(217, 70, 239, 0.18);`
* **Свечение активных икон:**
  `filter: drop-shadow(0 0 7px rgba(192, 80, 255, 0.65));`
* **Фоновое пятно (Blur Spot):**
  `background: rgba(147, 51, 234, 0.18); filter: blur(80px); width: 220px; height: 220px;`

### Границы (Borders)
* **Обычная:** `border: 1px solid rgba(255, 255, 255, 0.07);`
* **Активная:** `border: 1px solid rgba(168, 85, 247, 0.48);`
* **Премиум:** `border: 1px solid rgba(217, 70, 239, 0.38);`

---

## Компоненты UI

### 1. Скругления (Border Radius)
* **Большие карточки и модальные окна:** `20px` – `24px`;
* **Обычные карточки и списки:** `16px` – `18px`;
* **Кнопки и поля ввода:** `14px` – `18px`;
* **Теги и аватары:** `20px` – `30px`.

> **Правило:** Чем больше габариты блока, тем больше его радиус скругления.

### 2. Поля ввода (Inputs)
* **Фон:** `#101B28`;
* **Граница:** `1px solid rgba(255, 255, 255, 0.06)`;
* **Placeholder:** `#657080`;
* **Состояние Focus:** `border-color: rgba(168, 85, 247, 0.65); box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.12);`.

### 3. Мессенджер (Chat Bubbles)
* **Сообщение текущего пользователя:** Градиент `linear-gradient(135deg, #6D28D9 0%, #9333EA 100%)`, текст `#FFFFFF`.
* **Сообщение собеседника:** Фон `#17212D`, текст `#F2F4F7`.
* **Время сообщения:** `rgba(255, 255, 255, 0.42)`.

### 4. Теги и Интересы
* **Неактивный:** Фон `#131E2B`, граница `rgba(255,255,255,0.08)`, текст `#B4BBC6`.
* **Активный:** Фон `rgba(147, 51, 234, 0.2)`, граница `rgba(168, 85, 247, 0.65)`, текст `#E9D5FF`, свечение `0 0 12px rgba(147, 51, 234, 0.14)`.

### 5. Нижняя навигация (Bottom Bar)
* **Фон:** `rgba(6, 13, 22, 0.94)` (с эффектом `backdrop-filter: blur`);
* **Верхняя линия:** `1px solid rgba(255, 255, 255, 0.06)`;
* **Иконки:** Неактивная `#687281`, активная `#B05CFF` с подсветкой `drop-shadow(0 0 7px rgba(168, 85, 247, 0.55))`.

---

## Обработка медиаконтента (Фотографии)

Для сохранения премиального стиля все фотографии профилей должны иметь стилизацию:
* Сниженная насыщенность и слегка холодный баланс белого;
* Повышенный контраст и глубокие тени;
* Приглушённые зелёные и жёлтые оттенки;
* **Обязательный градиент затемнения снизу под текст:**
  ```css
  background: linear-gradient(
    to bottom,
    rgba(4, 10, 17, 0.02) 35%,
    rgba(4, 10, 17, 0.72) 75%,
    rgba(4, 10, 17, 0.98) 100%
  );
  ```

---
*Спецификация UI Design System • Cult Connection*